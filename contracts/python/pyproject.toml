[project]
name = "voicebridge-contract"
version = "0.1.0"
description = "VoiceBridge shared contract (Python mirror): event protocol + tool surface + demo ids"
requires-python = ">=3.10,<3.15"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["voicebridge_contract"]
