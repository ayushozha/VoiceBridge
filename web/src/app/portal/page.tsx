/**
 * Insurer Portal route (Agent 7 target).
 *
 * Scaffold placeholder. Agent 7 builds the Northstar dashboard here:
 * live call state, sponsor runtime trace, consent/audit log, outcome card —
 * an observer participant consuming the contract event stream.
 */
export default function PortalPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-5xl flex-col justify-center gap-4 px-6 py-16">
      <h1 className="text-2xl font-semibold">Northstar Insurance — Portal</h1>
      <p className="text-vb-muted">
        Scaffold placeholder — Agent 7 builds the insurer dashboard and runtime trace here.
      </p>
    </main>
  );
}
