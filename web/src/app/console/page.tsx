/**
 * User Console route (Agent 6 target).
 *
 * Scaffold placeholder. Agent 6 builds the member-facing surface here:
 * intent input, consent quick actions, language switch, correction taps —
 * wrapped in a LiveKit RoomContext and driven by `useVoiceBridgeEvents`.
 */
export default function ConsolePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-4 px-6 py-16">
      <h1 className="text-2xl font-semibold">User Console</h1>
      <p className="text-vb-muted">
        Scaffold placeholder — Agent 6 builds the member-facing control surface here.
      </p>
    </main>
  );
}
